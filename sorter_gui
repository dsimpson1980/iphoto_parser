import sys
from PyQt4 import QtGui, QtCore


class SorterGUI(QtGui.QMainWindow):

    def __init__(self):
        super(SorterGUI, self).__init__()
        self.initUI()
        self.filepath = None

    def initUI(self):
        QtGui.QToolTip.setFont(QtGui.QFont('SansSerif', 10))

        self.setToolTip('This is a <b>QWidget</b> widget')

        open_btn = QtGui.QPushButton('Open', self)
        open_btn.setToolTip('Select an iphoto library to parse')
        open_btn.resize(open_btn.sizeHint())
        open_btn.move(50, 25)
        open_btn.clicked.connect(self.select_file)

        parse_btn = QtGui.QPushButton('Parse!', self)
        parse_btn.setToolTip('Parse selected iphoto library')
        parse_btn.resize(parse_btn.sizeHint())
        parse_btn.move(50, 50)

        quit_btn = QtGui.QPushButton('Quit', self)
        quit_btn.setToolTip('Quit iphoto parser')
        quit_btn.resize(quit_btn.sizeHint())
        quit_btn.move(50, 75)
        quit_btn.clicked.connect(QtCore.QCoreApplication.instance().quit)

        self.setGeometry(300, 300, 250, 150)
        self.setWindowTitle('Tooltips')
        self.show()

    def select_file(self):
        self.filepath = QtGui.QFileDialog.getOpenFileName(self, 'Open file',
                '/home')


def MainWindow(app):
    sys.exit(app.exec_())


def main():
    app = QtGui.QApplication(sys.argv)
    sorter = SorterGUI()
    MainWindow(app)

if __name__ == '__main__':
    main()